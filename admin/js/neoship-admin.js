/**
 * Admin js file
 *
 * @package    Neoship
 */

( function ( $ ) {
	'use strict';

	$( document ).ready(
		function () {
			$( 'a.neoship-tracking' ).attr( 'target', '_blank' );
		}
	);

} )( jQuery );
